- content_for :content do
  #content
    .secondary
      .container
        = content_for?(:static_page) ? yield(:static_page) : yield

  = render "partials/footer", position: "static"

= render :template => 'layouts/application'
