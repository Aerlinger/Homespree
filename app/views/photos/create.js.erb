$("#portfolio-carousel ul.slides").append(
		"<%= j(render @photo) %>"
);

$("#portfolio-slider ul.slides").append(
		"<%= j(render @photo) %>"
);

$("#portfolio-carousel ul.slides").flexslider();
$("#portfolio-slider ul.slides").flexslider();