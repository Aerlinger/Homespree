$("#portfolio-carousel ul.slides").append(
		"<%= j(content_tag_for(:li, @photo) { render @photo, photo: @photo }) %>"
);

$("#portfolio-slider ul.slides").append(
		"<% j(content_tag_for(:li, @photo, class: "item") { render @photo, photo: @photo }) %>"
);