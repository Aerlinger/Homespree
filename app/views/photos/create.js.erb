/**
$("#portfolio-carousel ul.slides").append(
	"<%= j(render @photo) %>"
);

$("#portfolio-slider ul.slides").append(
	"<%= j(render @photo) %>"
);

$('#portfolio-carousel').flexslider({
	animation: "slide",
	controlNav: false,
	animationLoop: true,
	slideshow: false,
	itemWidth: 100,
	itemMargin: 5,
	asNavFor: '#portfolio-slider'
});

$("#portfolio-slider").flexslider({
	animation: "slide",
	controlNav: false,
	animationLoop: true,
	slideshow: false,
	sync: "#portfolio-carousel",
})
*/

window.location.reload(true);