$("li#<%= string_to_id(@specialty.name) %>").hide(500, function () {
	$(this).remove();
});

$("#contractor_specialties").prepend(
	'<%= j(
		render partial: "specialty", locals: {specialty: @specialty}
	)%>'
).fadeIn(500);