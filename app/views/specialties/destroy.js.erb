$("li#specialty_<%= @specialty.id %>").hide(500, function () {
	$(this).remove();
});
$("#available_specialties").prepend(
	'<%= j render partial: "available_specialty", locals: {specialty_name: @specialty.name} %>'
).fadeIn(500);